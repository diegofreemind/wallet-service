const { getAvailability, getScheduler, createScheduler, updateScheduler } = require('./ScheduleController');

module.exports = {
    getScheduler,
    updateScheduler,
    createScheduler,
    getAvailability
};