const { getAvailability, getScheduler, createScheduler, updateScheduler, createEvent } = require('./ScheduleController');

module.exports = {
    createEvent,
    getScheduler,
    updateScheduler,
    createScheduler,
    getAvailability
};